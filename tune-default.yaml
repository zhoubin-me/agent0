cluster_name: local-default
provider:
    type: local
    head_ip: 10.80.43.126
    worker_ips: [10.80.42.223]
auth: {ssh_user: bzhou, ssh_private_key: ~/.ssh/id_rsa}
## Typically for local clusters, min_workers == max_workers.
min_workers: 3
max_workers: 3
setup_commands:  # Set up each node.
    - pip install ray torch torchvision tabulate tensorboardX
